<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.broadcastreciever">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BroadCastReciever">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        aeroplene mode is active it is showing tht ->
<!- These are only present below oreo OS -->
<!--        <receiver android:name=".AirplaneModeReciever">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.AIRPLANE_MODE"/>-->
<!--            </intent-filter>-->
<!--        </receiver>-->
<!--   sender here is the system, it will display on in the version greater then 27    -->
    </application>

</manifest>